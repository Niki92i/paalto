# A11y review — First-run demo  *(reference output, WCAG 2.2 AA)*

**Verdict:** 3 findings, **1 BLOCKER.** Recommended action: **fix blocker, address majors before launch.**

| Frame | Issue | WCAG ref | Severity | Fix |
|---|---|---|---|---|
| 1/3 Empty dashboard | Secondary link "I'll start from scratch" — grey `#9A9A9A` on bone `#F4F1EA` measures 3.8:1, below 4.5:1 | 1.4.3 Contrast (Minimum) | **BLOCKER** | Switch link color to `--ink-2` (computed ratio 7.2:1) or underline + bolder weight. |
| 2/3 Demo project | Pulse animation runs continuously without a `prefers-reduced-motion` alternative defined in the spec | 2.3.3 Animation from Interactions | MAJOR | Wrap pulse in a media query: pause animation when `prefers-reduced-motion: reduce`. |
| 3/3 Post-aha toast | Auto-dismiss at 6s with no user-controllable pause | 2.2.1 Timing Adjustable | MAJOR | Add a "Pause" affordance on hover/focus, OR keep the toast visible until interacted with. Inline link is already focusable — good. |

**Passes (no action needed):**
- Hit targets — all interactive elements ≥ 24×24.
- Focus order — header → hero → CTA → secondary link → footer (logical).
- Form labels — n/a (no forms in this flow).
- 200% zoom — layout reflows without clipping at 1280px and 375px.
